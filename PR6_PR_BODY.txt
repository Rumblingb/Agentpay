Backend-only PR to tighten the `POST /api/merchants/payments/:transactionId/verify` response contract for Phase 1. What changed - Returns a more stable, authoritative verification response shape. - Keeps `paymentId` naming as-is. - Adds/surfaces verification fields needed by SDK consumers and integrators. - Keeps `proof` explicitly `null` for Phase 1. - Focused route tests pass. - Backend typecheck passes. Scope - Backend only - No SDK, dashboard, Stripe, prisma, or docs changes - No dependency or tooling changes in this PR Validation - Focused verify-route tests passed - Backend typecheck passed Why PR4 and PR5 strengthened settlement truth and downstream reliability. PR6 now tightens the verification contract so consumers can rely on a stable server response.

Next: PR7: first developer happy path
