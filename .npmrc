# Root-level npmrc — applied by EAS when installing from monorepo root.
# legacy-peer-deps prevents ERESOLVE failures from react-native version
# conflicts between workspace packages during EAS builds.
legacy-peer-deps=true
