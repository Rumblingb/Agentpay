#!/bin/bash
set -euo pipefail
source "$(dirname -- "$0")/_stack-common.sh"
message="$(stack_read_message "$@")"
if [[ -z "${message// }" ]]; then
  echo "Usage: bill-ask \"message\"" >&2
  exit 2
fi
inbox="$(stack_bill_workspace)/INBOX.md"
stack_append_entry "$inbox" "Founder request" "$message"
printf 'Wrote Bill inbox: %s\n' "$inbox"
